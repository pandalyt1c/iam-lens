# IAM Lens — MEMORY

Operating contract is in `CLAUDE.md` / `AGENTS.md`. This file is durable decisions / where we left
off; `git log` is the source of truth for what shipped.

IAM Lens is a STANDALONE brand at **iamlens.dev** (Next.js app) — never "permitted.io/iam".

Reference notes (user profile, project background, confirm-before-config feedback) live in this
project's Claude auto-memory at `~/.claude/projects/c--dev-iam-lens/memory/`, relocated there
2026-07-17 from a phantom `Cowork OS\iam-lens` path that never existed on disk.

_Decisions -> here; behavioral rules -> CLAUDE.md._
